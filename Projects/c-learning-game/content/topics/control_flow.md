Control Flow
------------
C programs execute statements in order, and branch or loop with control flow keywords.
Use if and else to conditionally execute code.
Use while and for to repeat actions. Break stops a loop; continue skips to the next iteration.
Use switch when you compare the same expression to many constant cases.

