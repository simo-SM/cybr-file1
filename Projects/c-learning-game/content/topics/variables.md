Variables and Types
-------------------
In C, a variable stores a value with a specific type, such as int, char, or double.
You declare a variable by writing its type followed by its name.
Example: int count = 0;
Common types: char for single bytes, int for integers, float and double for floating point, and unsigned variants.

Use const when a value should not change. Use descriptive names and initialize your variables.

