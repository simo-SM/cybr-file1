Functions
---------
Functions group reusable logic and can take parameters and return a value.
Declare a function with a return type, name, and parameter list. Example: int add(int a, int b) { return a + b; }
Functions can be declared in headers and defined in source files. Keep functions short and focused on a single task.

