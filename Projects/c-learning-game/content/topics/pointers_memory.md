Pointers and Memory
-------------------
A pointer holds an address of a value. Use the address-of operator & to obtain an address, and the dereference operator * to access the pointed value.
Dynamic memory is obtained with malloc and freed with free. Always free what you allocate and avoid using freed pointers.

