Arrays and Strings
------------------
An array holds a fixed number of elements of the same type. Its size is known at compile time for static arrays.
A string in C is a sequence of chars terminated by a zero byte. Use functions from string.h to work with strings.
Be careful with bounds. Never write past the end of an array or forget the terminating zero in strings.

